import React from 'react';
import {render} from 'react-dom';

var Menus = React.createClass({
    render: function () {
        return(
            <div id="menus">

            </div>
        );
    }
});

export default Menus;
